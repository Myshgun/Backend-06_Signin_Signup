<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0"
    />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <title><%= title %></title>
  </head>

  <body>
    <section class="h-100">
      <div class="container py-5 h-100">
        <% if (error) {%>
        <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <div class="row d-flex justify-content-center align-items-center h-100">
          <div class="col">
            <h3 class="mb-5 text-uppercase text-center">Заявки с формы</h3>
            <table class="table table-bordered">
              <thead>
                <tr>
                  <th scope="col">Дата отправки</th>
                  <th scope="col">ФИО</th>
                  <th scope="col">Телефон</th>
                  <th scope="col">Проблема</th>
                </tr>
              </thead>
              <tbody>
                <% for (let i = 0; i < receptions.length; i++) { %>
                <tr>
                  <th><%= receptions[i].date %></th>
                  <td><%= receptions[i].name %></td>
                  <td><%= receptions[i].phone %></td>
                  <td><%= receptions[i].problem %></td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </section>
  </body>
</html>
